# 统一行尾：所有文本文件在仓库内一律 LF，检出时也是 LF。
# 目的：避免不同机器 core.autocrlf 设置不同导致同一批文件被反复"修改"。
* text=auto eol=lf

# 二进制文件明确排除，禁止任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.webm binary
*.ico binary
*.pdf binary
*.zip binary
*.rar binary
*.7z binary
